<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="#">E-commerce</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <button
                class="nav-link btn btn-link"
                onclick="location.href='/'"
              >Home</button>
            </li>
            <li class="nav-item">
              <button
                class="nav-link btn btn-link"
                onclick="location.href='/api/products'"
              >Products</button>
            </li>
            <li class="nav-item">
              <button class="nav-link btn btn-link">
                <a
                  href="/api/users/uploadDocuments"
                  style="text-decoration:none;font-weight:normal;color:#9b9b9b;"
                >Cargar documentacion</a>
              </button>
            </li>
            <li class="nav-item">
              <button
                class="nav-link btn btn-link"
                onclick="location.href='/api/users/premium/{{_id}}'"
              >Change to rol</button>
            </li>
            <li class="nav-item">
              <button
                class="nav-link btn btn-link"
                onclick="location.href='/api/session/logout'"
              >Logout</button>
            </li>
          </ul>
        </div>
      </nav>
<div class="containerPrincipal" style="text-align:center;">
  <h1>Your Purchase Details</h1>

  <p>Please keep the ticket code
    <strong>{{code}}</strong>
    for tracking purposes.</p>
  <p><strong>Purchase Date and Time:</strong> {{purchase_datetime}}</p>
  <p><strong>Purchaser Email:</strong> {{purcharser}}</p>
  <p><strong>Purchase Status:</strong> {{status}}</p>

  <h2>Products Purchased:</h2>
  <div class="container">
    <div class="row">
      {{#each products}}
        <div class="col-lg-3 col-md-4 col-sm-6 col-12 mb-4">
          <div class="card" style="width: 100%;">
            <img
              class="card-img-top"
              src="{{pid.thumbnail}}"
              alt="Card image cap"
            />
            <div class="card-body">
              <h5 class="card-title">{{pid.title}}</h5>
              <p class="card-price">${{pid.price}}</p>
              <p class="_id">Code: {{pid.code}}</p>
            </div>
            <p style="color: white;text-align:center;">{{quantity}} units</p>
          </div>
        </div>
      {{/each}}
    </div>
  </div>
  <h2>Total Amount: ${{amount}}</h2>
  <p class="display-4">Thank you for your purchase! We appreciate your business.</p>
</div>
<script
  src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"
></script>